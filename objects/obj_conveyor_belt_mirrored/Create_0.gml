/// @description PROC GEN ONLY don't use in regular rooms

// Inherit the parent event
event_inherited();

//By default, start mirrored and move over
//Object is for PROC GEN ONLY
image_xscale = -1;
x = x + 16;